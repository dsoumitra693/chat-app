/**
 * Module exporting shared entities related to user and account management.
 *
 * The following entities are exported from the shared module:
 * - `userContacts`: Represents user contact information.
 * - `account`: Represents account-related data.
 * - `userSettings`: Represents user preferences and settings.
 * - `users`: Represents user data and profiles.
 *
 * These entities are used across different services for database operations and
 * other shared functionalities.
 *
 * @module sharedEntities
 */

export { userContacts, account, userSettings, users } from 'shared';
